{
  "name": "react-state-toolkit-example",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "setup": "cd ../ && yarn && yarn build && yalc publish && cd ./example && yalc link react-state-toolkit",
    "start": "yarn setup && react-scripts start"
  },
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "yalc": "^1.0.0-pre.53"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
